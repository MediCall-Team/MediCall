import 'package:flutter/material.dart';

class EditSPView extends StatelessWidget {
  const EditSPView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
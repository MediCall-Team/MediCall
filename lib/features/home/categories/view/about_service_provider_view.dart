import 'package:flutter/material.dart';

class AboutServiceProviderView extends StatelessWidget {
  const AboutServiceProviderView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("About"),);
  }
}